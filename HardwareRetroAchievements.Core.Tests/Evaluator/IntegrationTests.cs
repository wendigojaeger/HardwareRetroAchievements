using HardwareRetroAchievements.Core.Evaluator;
using System;
using System.Collections.Generic;
using System.Text;
using Xunit;

namespace HardwareRetroAchievements.Core.Tests.Evaluator
{
    public class IntegrationTests
    {
        //[Theory]
        //[InlineData("0xH0004==42", true)]
        //[InlineData("0xH0004==2", false)]
        //public void ShouldEvalEqualsMemoryWithConstValue(string input, bool expected)
        //{
        //    Parser parser = new Parser();
        //    var tree = parser.Parse(input);

        //    var evaluator = EvaluatorInstance.FromParserAST(tree);
        //    Assert.Equal(expected, evaluator.Evaluate());
        //}
    }
}
